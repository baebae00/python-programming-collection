# python-programming-collection
### A total of 25 problems

```python
#0. 변수 a에 정수 3을, 변수 b에 실수 4.5를 할당하고
#두 변수의 타입을 type함수와 print함수를 이용해 각각 출력하세요. [1점 - 기본점수]

a = 3
b = 4.5
print(type(a))
print(type(b))

#1. 정수와 실수의 사칙연산을 수행해봅니다.
#변수 add, sub, mul, div 에 각각 a에 b를 더한, 뺀, 곱한, 나눈 결과를 할당하세요.[2점]

add=a+b
sub=a-b
mul=a*b
div=a/b

#2. 하나의 print()함수를 이용해서 add, sub, mul, div를 모두 출력하세요. [2점]

print(add,sub,mul,div)

#3. 21을 9로 나눈 몫과 나머지 각각 f, g에 할당해주세요. [2점]

f=21/9
g=21%9

#4. print()함수를 이용해서 f의 g제곱을 출력해주세요. [2점]

print(f**g)

#5. 변수 A에 아래 내용을 가지는 문자열을 할당해주세요.
#I want to eat "mara-tang" [2점]

A="I want to eat \"mara-tang\""

#6. A에 저장되어 있는 문자열에서 슬라이싱을 이용해서 mara를 변수 B에,
#tang을 변수 C에 할당해주세요. [2점]

B=A[15:19]
C=A[20:24]

#7. B와 C를 이용해서 maratang이라는 문자열을 만들어 변수 D에 할당해주세요.[2점]

D=B+C

#8. a와 D를 이용해서 문자열 D를 a번 반복한 결과를 print() 함수로 출력해주세요. [2점]

print(D*a)

#9. input()함수를 이용하여 사용자로부터 정수를 입력받아 변수 E에 할당하고,
#D와 E를 사용하여 아래 문구를 출력하세요.
#                                   !!!띄어쓰기를 정확히 지켜주세요!!!
#maratang은 ~~~원 입니다.
#input() 함수에서 사용하는 안내문구는 자유롭게 작성하시면 됩니다. [2점]

E=int(input("가격을 입력하세요: "))
print(D,"은 ",E,"원 입니다.")

#10. 변수 L에 99,2,3,4,5 다섯 개의 숫자를 가지는 리스트를 할당해주세요.[2점]

L=[99,2,3,4,5]

#11. L의 0번 인덱스의 내용을 1로 변환해주세요.[2점]

L[0]=1

#12. L에서 슬라이싱을 이용하여 2,3,4 세 개의 숫자를 가지는 리스트를 변수 M에 할당해주세요.[2점]

M=L[1:4]

#13. for를 이용한 반복문을 이용해서 M에 저장된 세 숫자를 각각 문자열로 변환한 뒤,
#변환한 문자열을 이어붙인 결과 (문자열 '234')를 변수 N에 할당해주세요. [2점]

N=""
for m in M:
    N = N+ str(m)

#14. 13번 문제와 같은 방법으로 L에 저장된 다섯 숫자를 각각 문자열로 변환한 뒤,
#변환한 문자열을 이어붙인 결과 (문자열 '12345')를 변수 K에 할당해주세요. [2점]

K=""
for l in L:
    K = K+ str(l)

#15. 'K가 N보다 크다'는 의미의 조건을 만들고 print()함수를 이용해서 출력하세요. [2점]

print(K>N)

#16.사용자로부터 정수를 입력받아 변수 age에 할당해주세요.
#조건문을 이용하여 age에 할당된 값을 검사하여 
#age가 0이상 10미만일 경우 "어린이입니다." 라는 문자열을,
#age가 10 이상 20 미만일 경우에는 "청소년입니다." 라는 문자열을,
#age가 20 이상일때에는 "어른입니다." 라는 문자열을,
#age가 음수일때는 "잘못된 입력입니다."라는 문자열을 출력하도록 해주세요.
#나이를 입력받는데 사용하의 input함수의 안내문구는 자유롭게 작성하세요. [2점]

age=int(input("나이를 입력하세요: "))
if age>=0:
    if age<10:
        print("어린이입니다.")
    elif age<20:
        print("청소년입니다.")
    else:
        print("어른입니다.")
else:
    print("잘못된 입력입니다.")

#17. while문을 이용하여 1부터 10까지의 숫자를 출력해주세요.
#end 키워드를 적절히 사용하여 결과가 한 줄에 출력되고난 뒤 줄바꿈이 이루어지도록 해주세요. [2점]

i=1
while i<=10:
    print(i,end='')
    i+=1
print()

#18. for문을 이용해서 1부터 40까지의 숫자를 출력하는데,369 게임의 규칙에 따라
#숫자에 3,6,9가 포함되어있다면 숫자대신 문자열 '짝'을 출력해주세요.
#만약 3,6,9가 두개 포함되어있다면 짝도 두 번 출력되어야 합니다.
#총 40개의 숫자에 대한 결과를 출력하게 되는데,
#한 줄에는 10개의 결과가 출력되도록 end를 적절히 사용해주세요.
#즉, 1~10까지의 숫자/짝을 한줄, 11~20까지를 한줄, 21~30까지를 한줄, 31~40까지를 한줄에
#출력해 주시면 됩니다. [5점]

for num in range(1,41):
         j=str(num)
         if '3' not in j and '6' not in j and '9' not in j:
             if j=='10' or j=='20' or j=='40':
                 print('',j)
             else:
                 print('',j,end="")
         else:
             if j=='33' or j=='36' or j=='39':
                 print('',"짝짝",end="")
             else:
                 if j=='30':
                     print('',"짝")
                 else:
                     print('',"짝",end="")

#19. 두 개의 숫자(정수 혹은 실수)를 인자로 입력받아 그 합을 리턴하는
# add 함수를 작성해주세요.[2점]

def add(x,y):
    return x+y

#20. 두 개의 숫자(정수 혹은 실수)를 인자로 입력받아 그 곱을 출력하는
# mul_print 함수를 작성해주세요.[2점]

def mul_print(z,w):
    print(z*w)

#21. add함수를 이용해서 99와 127을 더하고, 그 결과를 변수 W에 할당 후 출력하세요.[2점]

def add(x,y):
    return x+y
W=add(99,127)
print(W)

#22. mul_print 함수를 이용하여 99와 127의 곱을 출력하고,
#함수의 결과를 변수 X에 할당 후 출력하세요. [2점]

def mul_print(z,w):
    print(z*w)

X=mul_print(99,127)

#23. random 모듈의 randint() 함수를 이용해서
#1이상 10000이하의 총 만개의 숫자 중,
#임의의 제곱수(어떤 자연수의 제곱이 되는 수)를 리턴하는 함수를 작성하세요.
#pass 부분을 삭제하고 코드를 작성하면 됩니다.[5점]

import random
def square():
    n=random.randint(1,100)
    result=n*n
    return result

#24. 다음 조건을 만족하는 수열 B가 있습니다
#      B(n)=B(n-1)*3+2, B(0)=1
#매개변수 n에 0 이상의 정수가 주어졌을 때에는 수열 B의 n번째 항의 값을,
#음수가 주어졌을 때에는 -1을 리턴하는 함수B(n)을 완성하세요.
#pass를 지우고 작성하면 됩니다. [2점]

def B(n):
    if n==0:
        return 1
    elif n>=0:
        return B(n-1)*3+2
    else:
        return -1

#25. 사용자로부터 임의의 개수의 숫자를 입력받아서
#입력받은 숫자의 평균, 분산, 표준편차를 출력하는 함수를 stat을 완성해주세요.
#평균, 분산, 표준편차를 구하는 식은 아래 사이트의 모 표준 편차를 참조하세요.
#
#    https://namu.wiki/w/%ED%91%9C%EC%A4%80%20%ED%8E%B8%EC%B0%A8#s-2.1
#
#이번 문제에서는 조건문과 반복문, 기본적인 연산자(+,-,*,/,**) 이외의 기능 사용을 금지하겠습니다. [5점]

def stat(*num):
    mean = None
    var = None
    std = None

    total=0
    length=0
    for x in num:
        total+=x
        length+=1
        mean=total/length

    v=0
    for x in num:
        v+=(x-mean)**2
        var=v/float(length)
        std=var**(0.5)

    print('평균:',mean)
    print('분산',var)
    print('표준편차',std)

stat(1,2,3)

```
